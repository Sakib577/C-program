#include <stdio.h>

int main()
{
    int width = 5;
    int hieght = 7;

    int area = width * hieght;
    int perimeter = 2 * (hieght + width);
    printf("Area= %d\n", area);
    printf("perimeter = %d", perimeter);
    return 0;
}
